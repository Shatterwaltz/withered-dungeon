Simple starter project for multiplayer games. 
It is set up to run two client instances, and one headless instance with the "dedicated_server" flag set.
Basic main menu with ip address field, connecting to server and toggling ready.
When all connected players are ready, game will run start_game() inside network.gd
# withered-dungeon
